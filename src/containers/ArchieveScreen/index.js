import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const ArchieveScreen = () => (
  <View style={styles.container}>
    <Text>Archieve Screen</Text>
  </View>
);

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingLeft: 10,
    paddingRight: 10,
  },
});

export default ArchieveScreen;
